{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "stet=$(\"${CLAUDE_PLUGIN_ROOT}\"/scripts/bootstrap.sh --quiet) && \"$stet\" build --detach all"
          }
        ]
      }
    ]
  }
}
