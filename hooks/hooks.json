{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/scripts/ensure-vale.sh --quiet && \"${CLAUDE_PLUGIN_ROOT}\"/scripts/ensure-data.sh --detach all"
          }
        ]
      }
    ]
  }
}
